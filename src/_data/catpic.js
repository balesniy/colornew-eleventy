const axios = require("axios");

module.exports = async () => {
    // const result = await axios.get("https://aws.random.cat/meow");
    // the API includes an image URL within the file key, and we are directly returning that value
    // out of our function within _data/catpic.js, and 11ty has made that output available globally via the filename
    // return result.data.file;
    return ''
};